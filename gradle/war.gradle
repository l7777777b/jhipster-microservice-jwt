apply plugin: "war"

bootWar {
    mainClassName = "com.mycompany.myapp.JhipsterMicroserviceJwtApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
